<div class="page-header">
  <h1>Bonus</h1>
</div>
<form action="/bonuses" method="post">
  <div class="form-group">
    <select name="user" id="user-select">
      <% User.all.each do |x| %>
        <option value="<%= x.name %>"><%= x.name %></option>
      <% end %>
    </select>
  </div>
  <div class="form-group">
    Points
    <input name="points" type="number">
  </div>
  <div class="form-group">
    <label>
      Notes
      <textarea name="notes" title="Notes" cols="30" rows="4"></textarea>
    </label>
  </div>
  <input type="submit" class="btn btn-default btn-primary" value="Send">
</form>
